# Excluded from the image (too large to bake in). Only present at container
# runtime if bind-mounted (docker-compose does this by default); otherwise the
# app falls back to demo mode. See docs/RUNNING_WITH_REAL_DATA.md.
data/raw/
data/processed/
data/artifacts/
models/

# Version control and CI
.git/
.github/
.dvc/cache/
.dvc/tmp/

# Python caches and virtual envs
__pycache__/
*.py[cod]
venv/
env/
.env

# Notebooks and local editor files
notebooks/
.ipynb_checkpoints/
.vscode/
.idea/
